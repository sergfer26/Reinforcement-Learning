#!/usr/bin/env python3
from base_agent import BaseAgent

class Human(BaseAgent):
    def select_action(key):
        board.show_board()
        action = input('Tu jugada es: ')
        return action

        
        
